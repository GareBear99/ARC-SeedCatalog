# Category Maps

Category maps translate messy source labels into canonical ARC category paths.

Example:

```json
{
  "movie": "media/movie/authorized",
  "show": "media/show/authorized",
  "public-domain": "media/public-domain/authorized"
}
```

The category map is hashed and referenced by receipts.
