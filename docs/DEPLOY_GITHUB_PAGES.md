# Deploy to GitHub Pages

```bash
git init
git add .
git commit -m "Initial ARC-SeedCatalog v0.5"
gh repo create GareBear99/ARC-SeedCatalog --public --source=. --remote=origin --push
```

Then enable Pages using the included workflow.
