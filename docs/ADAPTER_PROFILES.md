# Adapter Profiles

Adapter profiles describe how to read flexible JSON without changing code.

They define:

- server array paths
- item array paths
- field maps
- export_raw_fields: false

See:

```text
examples/adapter_profiles/generic_server_items.profile.json
```
